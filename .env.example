# ── Frontend (Vite) ──────────────────────────────────────────────────────────
# URL of the backend API. Leave empty to use the Vite dev proxy (recommended for local dev).
# Set to your deployed API URL for staging / production.
# VITE_API_URL=https://your-api.example.com

# ── Backend ──────────────────────────────────────────────────────────────────
# See backend/.env.example for backend environment variables.
